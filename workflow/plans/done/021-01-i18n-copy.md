# 021-01 — i18n copy (cancel swap + new keys)

Plan: `021-root-invoice-filter-and-ux-fixes.md` · Blocked by: — · Parallel-safe with: 02, 05

## Goal

One content.ts edit point: soften the 6 Burmese Cancel buttons, and add every new key the filter UI (03/04) + About page (06) will consume — keeping `en`/`my` symmetric.

## Context

- File: `apps/open-myanmar-invoice/src/lib/i18n/content.ts`. Structure: `const my = {…}` (lines 14–419) is the source of truth; `Content = typeof my` (line 422); `const en: Content = {…}` (428–827). **Both trees MUST stay symmetric** or typecheck breaks. Components read via `const t = useT()` (`src/stores/i18n-store.ts`).
- Existing relevant keys: `invoiceList.status.{draft,unpaid,paid}` (lines 90–94), `invoiceList.searchPlaceholder` (65), `invoiceList.noResults`/`noResultsHint` (78–79), `nav.{toHome,…,toSettings}` (97–108).
- This phase only edits copy — no component reads the new keys until phases 03/04/06. Define the exact key paths below so those phases bind to the same names.

## Steps

- [ ] **Cancel wording:** replace the exact string `ပယ်ဖျက်မည်` → `မလုပ်တော့ပါ` (6 occurrences in the `my` tree: `invoiceList.cancel`, `clients.cancel`, `products.cancel`, `products.quickAddCancel`, `invoices.builder.cancel`, `reports.export.cancel`). Use a replace-all on that exact value. **Do NOT touch bare `ဖျက်မည်`** (delete-action keys) — it's a substring of the cancel string, so only match the full cancel string. English unchanged.
- [ ] **Filter keys** — add a `filters` sub-object inside `invoiceList` (both trees). Status pills reuse `invoiceList.status.{draft,unpaid,paid}`; only "All" is new:
  - `statusAll` → MY `"အားလုံး"` / EN `"All"`
  - `statusGroupLabel` → MY `"အခြေအနေဖြင့် စစ်ထုတ်ရန်"` / EN `"Filter by status"`
  - `button` → MY `"စစ်ထုတ်ရန်"` / EN `"Filters"`
  - `createdRange` → MY `"ဖန်တီးသည့် ရက်စွဲ"` / EN `"Created date"`
  - `dueRange` → MY `"ပေးချေရမည့် ရက်စွဲ"` / EN `"Due date"`
  - `from` → MY `"မှ"` / EN `"From"`
  - `to` → MY `"အထိ"` / EN `"To"`
  - `clear` → MY `"ရှင်းမည်"` / EN `"Clear"`
  - `clearAll` → MY `"အားလုံး ရှင်းမည်"` / EN `"Clear all"`
  - `apply` → MY `"ပြီးပါပြီ"` / EN `"Done"`
  - `noMatch` → MY `"သင့်စစ်ထုတ်မှုနှင့် ကိုက်ညီသည့် ငွေတောင်းခံလွှာ မရှိပါ။"` / EN `"No invoices match your filters."`
- [ ] **Nav key:** add `nav.toAbout` (both trees) → MY `"အကြောင်း"` / EN `"About"`.
- [ ] **About block** — add a new top-level `about` block (both trees). URLs are NOT i18n (constants live in the page); only labels here:
  - `title` → MY `"အကြောင်း"` / EN `"About"`
  - `subtitle` → MY `"Open Myanmar Labs အကြောင်းနှင့် အက်ပ် အချက်အလက်"` / EN `"About Open Myanmar Labs & this app"`
  - `intro` → MY (purpose: open-source bilingual apps for Myanmar) / EN `"Open Myanmar Labs builds free, open-source, bilingual (English + Burmese) apps for Myanmar."`
  - `websiteLabel` → MY `"ဝက်ဘ်ဆိုဒ်"` / EN `"Website"`
  - `githubLabel` → MY `"ဆော့ဖ်ဝဲကုဒ် (GitHub)"` / EN `"Source code (GitHub)"`
  - `creatorLabel` → MY `"ဖန်တီးသူ"` / EN `"Created by"`
  - `creatorName` → `"Lwin Moe Paing"` (same both)
  - `facebookLabel` → `"Facebook"` (same both)
  - `appNameLabel` → MY `"အက်ပ်"` / EN `"App"`
  - `versionLabel` → MY `"ဗားရှင်း"` / EN `"Version"`
  - `check` → MY `"အပ်ဒိတ်များ စစ်ဆေးမည်"` / EN `"Check for updates"`
  - `checking` → MY `"စစ်ဆေးနေသည်…"` / EN `"Checking…"`
  - `upToDate` → MY `"နောက်ဆုံးဗားရှင်း ဖြစ်ပါသည်။"` / EN `"You're on the latest version."`
  - `updateAvailable` → MY `"ဗားရှင်းအသစ် ရရှိနိုင်ပါသည်။"` / EN `"A new version is available."`
  - `download` → MY `"ဒေါင်းလုဒ်လုပ်မည်"` / EN `"Download"`
- [ ] Keep `my` and `en` key trees identical; the Burmese above is a sensible default — refine wording if needed, just don't break symmetry.

## Done when

- All 6 `ပယ်ဖျက်မည်` are now `မလုပ်တော့ပါ`; no `ဖျက်မည်` (delete) string changed; English untouched.
- `invoiceList.filters.*`, `nav.toAbout`, and the `about` block exist in BOTH `my` and `en` with identical key paths.
- `bun run typecheck` (or `tsc --noEmit`) clean — proves symmetry.

## Touches

- `apps/open-myanmar-invoice/src/lib/i18n/content.ts` — cancel swap + new `invoiceList.filters`, `nav.toAbout`, `about` keys (both trees).

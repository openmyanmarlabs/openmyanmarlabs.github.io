# 009-07 — Developer docs

Plan: `009-root-data-migration-seeding.md` · Blocked by: 05 · Parallel-safe with: 06

## Goal

A short `src/bun/db/README.md` documenting the migration + seeding + backup system so a developer building on the template can extend it correctly.

## Context

- Repo doc convention (root `CLAUDE.md`): **concise, clipped, grammar traded for brevity**. Bullets over prose.
- Document the final shapes from phases 01–05 (don't invent — match the built APIs).
- Parallel-safe with phase 06 (different file); depends only on the API surface settled by phase 05.

## Steps

- [ ] Write `src/bun/db/README.md` covering:
  - **Add a schema migration** — edit `schema.ts`, run `bun run db:generate`, forward-only (no `down`).
  - **Add a static bilingual seed** — `drizzle-kit generate --custom`, INSERT in the migration (`*_en`/`*_my`), runs once via journal.
  - **Add an idempotent code seed** — `seedXxx` in `seed/seeds.ts`, `onConflictDoNothing`, runs every launch.
  - **Add a first-install seed** — gate on `upgrade.firstRun` in `runSeeds`.
  - **Backup / restore / retention** — auto-backup before pending migrations, restore + native dialog on failure, keep last 3 under `<userData>/backups/`.
  - **Step-by-step guarantee** — Drizzle's journal replays skipped versions in order.
  - **Caveats** — dev version is always `"0.0.0"`; default-admin seed is an illustrative security smell (prune/replace for real apps); `startup.ts` stays electrobun-free, `index.ts` owns the glue.

## Done when

- `README.md` exists at `src/bun/db/` and covers all four how-tos plus the rules/caveats, matching the built APIs.

## Touches

- `apps/electrobun-template/src/bun/db/README.md` — new doc.

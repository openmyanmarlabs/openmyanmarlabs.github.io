# 019-03 — i18n copy (`reports` group)

Plan: `019-root-report-page.md` · Blocked by: — · Parallel-safe with: 01, 02

## Goal

Add all bilingual copy the report page + export UI need to `content.ts` under a new
`reports` key group, plus `nav.toReports`. Keeps `en`/`my` key trees symmetric (a compile
error otherwise).

## Context

- File: `apps/open-myanmar-invoice/src/lib/i18n/content.ts`.
- `my` is the **source of truth**; `Content = typeof my`; `en` is typed `Content` → any
  asymmetry fails to compile. **Myanmar-first**: add the key to `my` first, then mirror in
  `en`. Append a new top-level `reports` group (don't rewrite existing groups). Also add
  one key to the existing `nav` group: `toReports`.
- Phases 04/05 reference these **exact** keys — keep the tree below stable.
- No tests (copy only); correctness is the symmetric-keys compile check + the page
  rendering in 04/05.

## Key tree to add

Under `reports` (both `my` and `en`):

```
reports: {
  title, subtitle, loading,
  period: { month, threeMonths, sixMonths, year },
  stats: { totalBalance, paidAmount, invoiceCount },
  charts: { monthlyTitle, statusTitle, total, paid, unpaid, empty },
  export: {
    byYear, byRange,
    yearTitle, yearSubtitle,
    rangeTitle, rangeSubtitle, from, to, submit, cancel,
    exporting, exported, exportError, rangeInvalid,
  },
}
```

Plus `nav.toReports`.

## Steps

- [ ] In `my`, add `nav.toReports: "အစီရင်ခံစာ"`. Mirror in `en`: `toReports: "Reports"`.
- [ ] In `my`, add the `reports` group (Myanmar). Suggested values (refine for natural
      Burmese — clipped, consistent with the existing tone):
  - `title: "အစီရင်ခံစာ"`, `subtitle: "သင့်လုပ်ငန်း၏ ဝင်ငွေ အနှစ်ချုပ်။"`, `loading: "ဖွင့်နေသည်…"`
  - `period`: `month: "၁ လ"`, `threeMonths: "၃ လ"`, `sixMonths: "၆ လ"`, `year: "၁ နှစ်"`
  - `stats`: `totalBalance: "စုစုပေါင်း လက်ကျန်ငွေ"`, `paidAmount: "ပေးချေပြီး ပမာဏ"`,
    `invoiceCount: "ငွေတောင်းခံလွှာ အရေအတွက်"`
  - `charts`: `monthlyTitle: "လအလိုက် ဝင်ငွေ"`, `statusTitle: "ပေးချေမှု အခြေအနေ"`,
    `total: "စုစုပေါင်း"`, `paid: "ပေးချေပြီး"`, `unpaid: "မပေးချေရသေး"`,
    `empty: "ပြသရန် ဒေတာ မရှိသေးပါ။"`
  - `export`: `byYear: "နှစ်အလိုက် ထုတ်ယူမည်"`, `byRange: "ရက်အပိုင်းအခြားဖြင့် ထုတ်ယူမည်"`,
    `yearTitle: "နှစ် ရွေးချယ်ပါ"`, `yearSubtitle: "ထုတ်ယူလိုသော နှစ်ကို ရွေးပါ။"`,
    `rangeTitle: "ရက်အပိုင်းအခြား ရွေးချယ်ပါ"`, `rangeSubtitle: "စမှ ဆုံးရက် ရွေးပါ။"`,
    `from: "မှ"`, `to: "အထိ"`, `submit: "ထုတ်ယူမည်"`, `cancel: "ပယ်ဖျက်မည်"`,
    `exporting: "ထုတ်ယူနေသည်…"`, `exported: "Downloads ဖိုလ်ဒါသို့ သိမ်းပြီးပါပြီ။"`,
    `exportError: "ထုတ်ယူ၍ မရပါ။ ထပ်စမ်းကြည့်ပါ။"`, `rangeInvalid: "စရက်သည် ဆုံးရက်ထက် နောက်ကျနေသည်။"`
- [ ] In `en`, mirror the same keys:
  - `title: "Reports"`, `subtitle: "An overview of your revenue."`, `loading: "Loading…"`
  - `period`: `month: "1 Month"`, `threeMonths: "3 Months"`, `sixMonths: "6 Months"`, `year: "1 Year"`
  - `stats`: `totalBalance: "Total balance"`, `paidAmount: "Paid amount"`, `invoiceCount: "Invoice count"`
  - `charts`: `monthlyTitle: "Monthly revenue"`, `statusTitle: "Payment status"`,
    `total: "Total"`, `paid: "Paid"`, `unpaid: "Unpaid"`, `empty: "No data to show yet."`
  - `export`: `byYear: "Export by year"`, `byRange: "Export by date range"`,
    `yearTitle: "Choose a year"`, `yearSubtitle: "Pick a year to export."`,
    `rangeTitle: "Choose a date range"`, `rangeSubtitle: "Pick a start and end date."`,
    `from: "From"`, `to: "To"`, `submit: "Export"`, `cancel: "Cancel"`,
    `exporting: "Exporting…"`, `exported: "Saved to your Downloads folder."`,
    `exportError: "Could not export. Please try again."`,
    `rangeInvalid: "Start date is after the end date."`
- [ ] Confirm the file type-checks (symmetric trees) — no `tsc` errors.

## Done when

- `content.ts` has a `reports` group + `nav.toReports` in both `my` and `en`, key-symmetric.
- App type-checks (the `en: Content` constraint passes).

## Touches

- `apps/open-myanmar-invoice/src/lib/i18n/content.ts`

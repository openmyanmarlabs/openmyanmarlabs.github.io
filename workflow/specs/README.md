# Specs

Specification docs. What to build + requirements, scope, constraints.
Comes after `../ideas`, leads to `../plans`.

One file per spec: `YYYY-MM-DD-<slug>.md` (suffix `-HHMM` if same-day).

Written by the `spec` skill (`/spec`) — interviews you, then writes here. Feeds `plan-maker`.
